name: Automation Pipeline

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    env:
      BASE_URL: ${{ secrets.BASE_URL }}
      API_KEY: ${{ secrets.API_KEY }}
      APPIUM_SERVER_URL: ${{ secrets.APPIUM_SERVER_URL }}
      ANDROID_DEVICE_NAME: ${{ secrets.ANDROID_DEVICE_NAME }}
      ANDROID_APP_PATH: ${{ secrets.ANDROID_APP_PATH }}

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout repo
        uses: actions/checkout@v4

      # 2️⃣ Set up JDK
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # 3️⃣ Cache Maven dependencies
      - name: Cache Maven packages
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}

      # 4️⃣ Install Chrome + Chromedriver
      - name: Install Chrome and Chromedriver
        run: |
          sudo apt-get update
          sudo apt-get install -y wget unzip xvfb libxi6 fonts-liberation \
            libnss3 libxss1 libappindicator3-1 libatk-bridge2.0-0 libgtk-3-0 \
            libasound2 libpangocairo-1.0-0 libcups2 libdbus-1-3 libxcomposite1 \
            libxcursor1 libxdamage1 libxrandr2 libxinerama1 libfontconfig1 \
            curl gconf-service gconf2
          
          wget -q -O /tmp/google-chrome.deb https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
          sudo dpkg -i /tmp/google-chrome.deb || sudo apt-get install -f -y
          
          CHROME_VER=$(google-chrome --version | awk '{print $3}' | cut -d. -f1)
          echo "Chrome version: $CHROME_VER"
          
          LATEST=$(curl -sS "https://chromedriver.storage.googleapis.com/LATEST_RELEASE_$CHROME_VER")
          wget -q -O /tmp/chromedriver.zip "https://chromedriver.storage.googleapis.com/$LATEST/chromedriver_linux64.zip"
          sudo unzip -o /tmp/chromedriver.zip -d /usr/local/bin
          sudo chmod +x /usr/local/bin/chromedriver
          
          # Verify
          google-chrome --version
          chromedriver --version

      # 5️⃣ Build and run tests
      - name: Build and Run Tests
        run: mvn clean test -B

      # 6️⃣ Generate Allure Report (optional)
      - name: Generate Allure Report
        run: mvn allure:report

      # 7️⃣ Upload Allure Report
      - name: Upload Allure Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: target/site/allure-maven-plugin

      # 8️⃣ Upload Extent Report
      - name: Upload Extent Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: extent-report
          path: target/extent-reports/ExtentReport.html

      # 9️⃣ Upload Cucumber HTML Report
      - name: Upload Cucumber HTML Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cucumber-report
          path: target/cucumber-reports/cucumber.html

      # 🔟 Upload Surefire Reports
      - name: Upload Surefire Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: surefire-reports
          path: target/surefire-reports
